/* The cool waterfall video in public as a background */
import React from 'react';
import { useEffect, useState } from 'react';
import { WaterfallVideo } from './WaterfallVideo';

export const Homepage: React.FC = () => {
    return (
        <WaterfallVideo />
    );
}